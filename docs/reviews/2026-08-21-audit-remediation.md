# Audit remediation ledger

Tracks every finding in [assessment.md](../../assessment.md) (greybeard audit at
HEAD a6a267e2, 2026-08-20) from raised to closed. One row per appendix finding,
in the audit's own order, so `A<n>` is a stable handle across sessions.

**Status vocabulary** — deliberately narrow, so the ledger cannot decay into the
state the audit caught `issues.md` in:

| Status | Meaning |
|---|---|
| `open` | Not started. |
| `wip` | Being worked now; a branch exists. |
| `done` | Landed on `main`, with the commit named. |
| `decision` | Blocked on a product/strategy call that is the maintainer's to make, not an engineering task. |
| `wontfix` | Deliberately declined, with the reason recorded. |

**The rule:** a row moves to `done` only when the fix is on `main` and the
evidence line names the commit. Anything else stays `wip`. If a fix turns out to
be wrong later, the row reopens rather than a new row being appended.

## Progress

<!-- progress -->

`███████████████████░░░░░` **42/53 closed** — 3 wip, 42 done, 8 decision

## Findings

| ID | Sev | Area | Status | Evidence / notes |
|---|---|---|---|---|
| A01 | C | Trust layer | `done` | PreparedCandidate::diagnostics() is now the one aggregate stream: quarantined documents surface as document-quarantined errors, and the three swallow arms in validate.rs return an analysis-failed diagnostic instead of an empty (=clean) vec. The vacuous rename all() assertion now asserts on errors. Predicted wave of latent doc-contract failures did NOT materialise: check docs/waml is still exit 0, 2 pre-existing warnings. |
| A02 | C | Distribution | `decision` | Still the owner's call to CUT one, but the mechanism now exists: .github/workflows/release.yml packages a Windows zip (binary + LICENSE + THIRD-PARTY + README, with a SHA256 beside it) and the .vsix on a v* tag, and drafts a release so nothing goes public until a human presses publish. The CLI also answers --version, which it did not: a release asset that cannot say which build it is unsupportable. Verified as far as it can be without cutting a tag — the binary builds and runs, and the staging and hashing steps were executed locally. The end-to-end tag path is unexercised. DECISION REMAINS: cut v0.x, or amend mvp.md to defer native install. |
| A03 | C | Product definition | `decision` | Who adopts this and why. The audit's suggestion — agent-authored architecture docs reviewed by humans, per OKF_SPEC 1 — is unclaimed positioning, but naming the user reshapes the goal tree. |
| A04 | C | Accessibility | `decision` | Ship a plain-HTML export beside the canvas reader. Cheap relative to what exists and product-defining while absent, but it is a new product surface, not a defect fix. |
| A05 | C | Rendering gate | `done` | The Linux reference is committed (crates/waml-editor/tests/references/light-cycle.linux-x86_64.ink, 30KB, 45588 ink pixels), recorded by CI run 32458844571 and downloaded from its rendering-gate artifact — so the gate now ENFORCES on Linux rather than recording itself and passing. Everything else as recorded above: ink comparison rather than pixel values, argued against a tolerance that would have missed today's 6.4e-4 router change, proven against that change and against an antialias-only shift that correctly changes nothing. |
| A06 | H | CI safety net | `done` | Verified by four real runs, not by reading the workflow. Each round found something: a new syntax_edits crash, then TWO more (another syntax_edits plus the first outer_mapping crash ever, which surfaced only because the corpus gained committed seeds). All fixed. The outer_mapping one was the tip of a bug on well-formed documents — heading level was counted from the leading hash run without looking at the node kind, so a setext heading (Title over ===) published as level 2 and every 'level == 1' title lookup silently missed such a document. The last syntax_edits one was a guard anchored to the line start, so a reference definition inside a block quote or list item was invisible to it. Five crash inputs are committed as corpus seeds, because the corpus is cache-only on a weekly schedule and a cache miss starts from the checked-in seeds alone. |
| A07 | H | Data integrity | `done` | Corrected: NOT live data loss — the only wire field (AttrSet.mult) already carried skip_serializing_if and round-trips Unchanged correctly (verified). The real defect was the latent trap: serializing Unchanged emitted null, so any future field forgetting the attribute would silently turn 'leave alone' into 'delete'. Unchanged now fails to serialize with a message naming the fix; three-state round-trip pinned by test. |
| A08 | H | Triage discipline | `done` | issues.md reconciled 2026-08-21: the three P1s verified fixed since the freeze are recorded with their commits (nesting overflow, FieldEdit wire, shell/frontmatter diagnostics) instead of silently deleted, the still-live LSP P1 is named, and the header points audit findings at this ledger so the two documents have distinct jobs. |
| A09 | H | Doc contract | `done` | Re-gated by A01: the contract's command now reports quarantined documents and analysis failure. Verified after the fix — docs/waml is genuinely clean, not silently clean. CI step ordering is tracked separately as A35. |
| A10 | H | Supply chain | `done` | cargo-deny (advisories+licenses+bans+sources) now runs as a CI job with deny.toml; it immediately found and fixed two real quick-xml DoS advisories (RUSTSEC-2026-0194/0195) and a yanked roughr-merman, and every Action is SHA-pinned with the tag in a trailing comment. The ttf-parser/rustybuzz unmaintained advisories are ignored with a written reason (same SVG text stack, no safe upgrade). Single-account risk is A24/A48, a decision row. |
| A11 | H | Layering | `done` | Made true rather than deleted: ViewDecl moved to okf::decl (it is authored frontmatter, not view machinery) and Bundle::resolved_view became a free function in view::chain. Production okf now imports nothing from the view/UML tiers, enforced by tests/okf_layering.rs (verified against a deliberate violation). |
| A12 | H | Layering | `wip` | Measured: 114 files name analysis::, but only FIVE couple to it for DocumentId alone — the other 109 want prepare_candidate and the pipeline, which is a facade being used as designed. DocumentId moved to crate::source (a document already has a durable name there), killing the analysis<->diagnostic cycle outright: diagnostic.rs now has zero analysis:: references. The finding's edit/ claim was half wrong — edit needs analyze_okf and DomainAnalysisContext, so no id move could ever decouple it; its real back-edge was the AnalysisError conversions, now in edit/code.rs beside the same impls for four other error types. The back-patching was fixed on the OkfAnalysis side rather than the doc, because okf.rs's own rule forbids importing UML types AND because attach_code_syntax made the value half-built: observed before that call it answered code_spans with an empty map, indistinguishable from 'no spans'. Remaining and NOT breakable by moving code: analysis <-> uml, which needs the pipeline vocabulary extracted into a tier below both — a crate split, not a module move. |
| A13 | H | Cohesion | `done` | 4,610 lines to ten modules in a strict DAG, verified a pure move by diffing the line multisets (nothing lost; every delta is a use/mod line, a super:: path rewrite, a pub(crate), or rustfmt rewrapping). The audit's four-way read was close but wrong in two places: extraction hides a leaf layer of syntax primitives that validation and projection also use, and 'projection' is four jobs with different consumers. Two more seams the audit did not name — diagnostic construction, and the shared verdicts the file's own doc already described as 'one verdict, two consumers'. Two apparent back-edges turned out to be doc-comment mentions rather than calls, so no cycle had to be forced and A12's hub is not made worse. analyze + Analysis + the island cache stay together at 908 lines deliberately: splitting them would mean widening exactly the private fields the file's visibility rule protects. |
| A14 | H | Performance ceiling | `done` | Measured, then fixed the three worst offenders. The syntax layer was already incremental and provably so — 199/200 markdown snapshots and 121/122 island trees reused on a one-character edit — but everything downstream threw that away and recomputed for all N documents. Per-edit cost was Theta(N) with ~100% of it edit-independent. Three correctness-preserving fixes, no new cache: validate_document_types was calling parse_markdown on the WHOLE document to get one token range, for every untyped document (76 of 180 in docs/waml); 15 separate tree walks became one fused walk; and okf::shell::derive no longer re-verifies pointer-identical documents. 200 documents: 102ms -> 14ms, a 7x speedup. The 60fps wall moved from ~65 documents to ~215 — and docs/waml is 180, so this repo's own architecture bundle was already past the frame-drop point. Benchmark at crates/waml/benches/bundle_reanalysis.rs, analysis in docs/design/bundle-reanalysis-cost.md. The remaining 40% is per-document DeclaredConcept construction, which cannot be memoised on a document's own syntax because declarations resolve cross-document — the correct key is (island tree identities, recognised concept id set), and it needs its own pass with invalidation tests. |
| A15 | H | App shell | `wip` | Cluster map in docs/design/app-god-object-clusters.md; App 54 -> 28 fields across nine clusters. Extracted: dock chrome (11), zoom (3), context-menu subject (2), palette (3), agent marks (3), the deferred-restore pair (2), projection+tree cache (4), open project+save (4), and the session-search pair (2). FIVE were latent bugs rather than shape, all now unrepresentable: a dismissed context menu's subject could be dispatched against the wrong row; the palette's state outlived the palette that filled it; the nav-tree cache key stored one field of ChainLimits while the build took the whole value, so a second field would have produced a silently stale tree; close_model left the closed project's name behind; and end_session_search left the step stamp behind. The map's C6 grouping was WRONG and is corrected in the doc: ViewHistory is already a type with nine methods, and pending_fragment's real siblings are pending_reveal and anchor_restore — a trio that cuts ACROSS C6 and C2. Before unifying that trio, settle whether a pending fragment should stay armed for a later visit (it does) or be dropped like a reveal (it is not) — the two differ and the doc claimed they did not. C4 web boot deliberately untouched: cargo test --workspace runs zero lines of it. |
| A16 | H | Fork seam | `done` | All four makepad fork deps hoisted into [workspace.dependencies] (lockfile byte-identical), and pages.yml now READS the rev out of Cargo.toml instead of carrying its own copy — it had drifted 10 days behind (62f515dc vs 6534634a). A CI equality check would have caught the drift; deriving it means there is nothing to drift. |
| A17 | H | Fork inventory | `done` | docs/makepad-fork.md inventories all 44 fork commits against upstream/dev, classified upstream-worthy (27) / product (9) / dead (8), with the regeneration command and the both-directions drift check. The wip: DComp commit and seven other dead carries are named as removable on the next rebase. |
| A18 | H | Wire contract | `done` | DocumentsRequest, RevisionResponse and ConflictResponse now live in waml-ops-dto beside DocumentWrite, so the editor's encoder and the CLI's decoder share one definition instead of two structurally-unlinked copies. A test pins the exact wire spelling of every field, because a round-trip through one side alone would not catch drift. |
| A19 | H | Solver depth | `done` | Coverage was real but blind to one axis: 32 route tests all asserted that constraints HOLD, and nothing could see quality. Nine rules now audit every scene (crates/waml/src/solve/route/quality.rs) — orthogonality, endpoint on border, stub perpendicular and at least ROUTE_MARGIN, corner inset, no segment through a node or through the route's own endpoint node, bounded path length and turn count, no two connectors overlapping on a shared run. They found FIVE real defects, four in the post-A* repair phases the finding predicted: A* could double back for free (dir_of dropped sign, so a reversal cost no bend and simplify folded it away, leaving a 19px stub where 24 is mandatory); attach wired stubs into their own node; hub_spread destroyed the opposite end's stub on a 3-point route where both ends share one bend — the same class fd8f305f fixed in nudge and left here; plain nudge had no obstacle model at all; and back edges in flow.rs, routed outside the router, all took their side's exact midpoint, so the SHIPPED activity fixture had two connectors overlapping 31px. Breaches 152 -> 52, all one class (shared runs on dense synthetic scenes), zero on authored fixtures. Visual sign-off owed as V14. |
| A20 | H | LSP | `done` | The ownership model is now the protocol's own and is stated in bundle.rs as a module doc: the client owns a file between didOpen and didClose, the disk owns it outside that, and disk_by_physical is a shadow of the last read rather than a fallback. The finding was accurate and understated — the startup map was written once and never again, so a file CREATED after boot was invisible and a DELETED one stayed in the bundle publishing diagnostics forever. A failing e2e test showed the corruption in one line (a closed document read back as its startup bytes, content the user never typed and the disk did not hold), and it passes now. didSave is advertised and handled but deliberately takes no text: DidSaveTextDocumentParams carries no version, so accepting its text would open a second unordered channel that could overwrite a newer didChange. Watched files are registered dynamically (the spec has no static capability), the handler ignores the event type and re-reads because watch events are advisory, and dot-directories are filtered so a write under .git cannot put a document in the bundle that the next restart would drop. Deletes now retract their diagnostics. Reported separately: a non-file: URI (untitled: buffers) is invisible to the server. |
| A21 | H | Verification debt | `done` | The sign-off ledger is now docs/reviews/visual-signoff-ledger.md — 13 rows, each naming the landed commit and the specific thing nobody has looked at. It also records what is already obsolete (V12's peek machinery was deleted today) and which row is a choice rather than a check (V13). The obligations stay open; what is closed is their living only in one agent's session memory. |
| A22 | H | Effort allocation | `decision` | Three company-sized products plus a forked framework, solo. Which artifact is THE product determines what gets cut. |
| A23 | H | Interop | `decision` | One-way mermaid/SVG export so diagrams can leave the toolchain. Scoped as a feature, not a fix. |
| A24 | H | Sustainability | `decision` | Bus factor 1 with contribution repelled. Declare a personal tool, or do the contribution minimum and upstream the fork's general patches. |
| A25 | M | Licensing | `done` | THIRD-PARTY.md added at the repo root and embedded into every assembled site (site.rs THIRD_PARTY_FILE), with a test asserting a site cannot ship without it. cargo-deny's licenses check keeps the allow-list honest. |
| A26 | M | Serve security | `done` | The serve trust model is stated where the flags are read: the token travels in a query parameter (browser history, proxy logs), there is no TLS, and --bind-all puts token, requests and document bodies on the network in cleartext with the Host check necessarily relaxed. The flag's own help repeats it. |
| A27 | M | Surfaces | `done` | The five hand-copied sibling lists are one const: BodyWidgets::DOCUMENT_SURFACES, with show_only() raising one and taking the rest down. The drift was a LIVE bug, not just duplication — five show_* never learned about behavior_canvas_wrap, so leaving an activity/state-machine/sequence tab for the folder view, either markdown surface, search results or the book left the behavior canvas on screen underneath. A test drives all six show_* with the behavior canvas up and was confirmed to fail before the fix. diagram_properties_wrap stays out of the list deliberately: it is a companion panel of the canvas, driven per mode by ClassDiagramView::sync, not a sibling competing for the centre. |
| A28 | M | ViewOutcome | `decision` | The bag is real: twelve fields, and view_source, open_folder_listing and tree_mark each exist because the navigation channel could not express them — their own doc comments say so. Fixing it properly means redesigning what NavigationTarget can say, which changes how every surface routes and can only be judged by driving the UI; that is an owner call, not an unattended refactor. Removed one artefact of the accretion: the popup field carried half of a deleted open_preview field's doc comment (orphaned in 1d3826f7). PROPOSED SHAPE for the decision: fold the three explicit-surface fields into a single NavigationIntent whose target names a surface as well as a document, leaving ViewOutcome with edit/source_edit/popup/navigation plus the three shell-side flags. |
| A29 | M | Solve pipeline | `done` | The three lists are one Vec<DrawableEdge> holding the edge with its two endpoint keys, so the hand-maintained index alignment is gone and the zip with it; a second shadowed cluster pre-solve was renamed apart. The debug_assert stays but now guards something else: one of its two failure arms is structurally impossible, and the other (clipping collapsing a route to a point when nodes overlap) is a real geometric degeneracy. No golden moved. |
| A30 | M | Solve API | `done` | Eleven entry points to five. The audit undercounted: THREE of the four route tiers had zero production callers, not two, and stress::layout/layout_grouped and three of the four label entry points were production-dead too. They collapsed rather than gaining a facade, because they were never four operations — each stack was one implementation plus wrappers whose whole body spelled a default argument, and a facade over that just adds a fifth name. Also removed a shipped lie: route_keyed_with took a &SolveConfig it never read, underscore-prefixed in the public signature, threaded through every tier by every caller. |
| A31 | M | Error handling | `done` | EditError's reason was the only stringly part — index, op and selector were already structured. 114 sites grouped into 10 kinds, so the enum wrote itself: EditCode, declared once per variant in a macro table with ALL, as_str and the serde rename expanding from the same literal, exactly the diag_codes! mechanism. Deliberately NOT folded into DiagCode, and the argument is in the code: a diagnostic describes a document and carries a range and a severity, an edit error describes a rejected operation and has neither. Every message string is byte-identical; a subject field lifts 52 targets out of the prose, and EditError::wrap replaces 33 sites that flattened an already-classified error to its sentence — each impl an exhaustive match, so a new upstream variant is a compile error rather than a mis-coded rejection. BundleError::Analysis(String) became a code plus message from an exhaustive AnalysisError::code(). The 422 body gained code/op/index/subject beside the unchanged error and reason, so no client breaks. One test was string-searching a Debug dump for a variant name — that discriminant existed, was destroyed, and was being grepped back out of prose. |
| A32 | M | cfg seams | `done` | 54 gates to 9 in markdown_extensions, 162 to 110 workspace-wide. The seam ran THROUGH EditorMarkdownExtensionHost — it held the native pool AND the cooperative queue, so every method had to be gated individually, which is why nothing was adjacent enough to group. A RenderScheduler trait with one impl per file (each gated once, at its mod declaration) plus a shared RenderLedger moves the seam to a boundary, and default_scheduler() is the same one-name-two-bodies facade config::now_unix already uses. The native-test-compiles-the-cooperative-path property still holds, which matters because it is the wasm path's only automated coverage. Honest limit: 45 of the 52 removed came from this file plus browser_boot; the remaining 110 are per-behaviour gating that no facade improves, and forcing one on the app shell would be the indirection-for-its-own-sake the finding does not need. |
| A33 | M | Dead code | `done` | 190 allow sites -> 59, of which 37 were always legitimate cfg_attr platform gates: the real suppressions went 153 -> 22, every survivor field-scoped and commented with its consumer. 40 items became compiler-enforced cfg(test). Peek deleted (~230 lines); radial collapsed to the one open mode that ships (~290 lines, 19 allows -> 0) — the audit was wrong that it was superseded, RadialOpen::Dial is live; the blanket DocView allow was hiding nothing. Harness binaries and node_design_editor are behind an off-by-default harness feature. Net -862/+290 across 59 files. |
| A34 | M | CI ordering | `done` | The documentation contract is now its own job instead of the first step of build-test, so it can no longer mask code failures behind it, and the pre-merge wasm check covers waml-editor — the crate that actually breaks the deploy (verified: it compiles for wasm32 today). |
| A35 | M | Panic hygiene | `done` | Census first: 136 unwraps, but ~104 were provably unreachable GreenFactory calls — the count oversells the risk. Content-reachable ones fixed at the source (slice clamps spans into the island and floors both ends to UTF-8 boundaries, diagnostic ranges are ordered, all eight bad_token recovery sites route through a helper that handles the empty span). catch_unwind guards prepare_candidate and the LSP's five query and three ingest paths. CORRECTION on the wasm half: wasm32-unknown-unknown is panic-strategy abort (verified), so catch_unwind catches nothing there. CORRECTION ON THE CENSUS ITSELF, found by a later fuzz round: it classified ~90 source[a..b] slices unreachable because 'every +1/+2 is guarded by a starts_with on an ASCII literal'. A re-audit of all 150 offset sites found 129 genuinely guarded and FOURTEEN not, in five flow-parser functions reached from behavior_bounds — three of which panicked on a multi-byte character and one of which silently mis-sliced. All fixed; the rule is now stated as step over a character, not a byte. |
| A36 | M | Frontmatter grammar | `done` | One scanner, not three — the finding undercounted: there were EIGHT hand-rolled copies of the frontmatter quoted-scalar scan in three behavioural shapes, and all eight now call waml_syntax::scan_quoted_scalar. It steps whole characters, so the character-boundary guarantee is structural rather than remembered. A differential proptest over 600k cases showed the copies agreed only by luck: their escape handling differs exactly where the differing byte cannot close a quote (hex digits, UTF-8 continuation bytes), so any one of them growing a diagnostic or a token span would have re-created 5dbb73c2's mid-sequence range. One deliberate difference survives, documented: the reader honours a quote only at item start (YAML's rule), the rewrite guard treats any quote as a delimiter — fail-closed, and caller policy rather than a scanner parameter. |
| A37 | M | UI-test harness | `done` | The ratio complaint had the wrong numerator: 1,543 of the 3,822 lines are the harness's OWN test modules, so the harness is ~2,300 lines and a scenario costs 8-14. Verdict: right-sized but under-used, and reaching below its own ceiling — makepad_test exposes per-widget rects and cursor stepping that WamlApp never surfaced, and ProjectTree::semantic_items had been emitting per-row rects that nothing asserted on. Three scenarios added for things that break quietly (exact tree row list with no zero-height or overlapping rows, selection landing AND staying in view, F3/Shift+F3 traversal with both wraps), plus crate docs stating what it can and cannot judge. Partly automates ledger rows V4 and V7. Also corrected a stale README claim: the headless runtime DOES work on Windows, which matters because that is the owner's dev machine. |
| A38 | M | Chain layering | `done` | Chain::build no longer name-checks 'hide': Projection::validate_declaration is a trait hook defaulting to Ok, so the stage owns its rule and the builder knows nothing about which middlewares have one. Landed 70b63253; the generic test was confirmed to fail against the old name-check. |
| A39 | M | Planning hygiene | `done` | 74 active plans triaged to 11 plus a README: 63 moved to completed/ with git mv, each verdict decided by artifact existence rather than checkboxes (the implement-plan runner records progress in Plan-Tasks trailers, so almost every shipped plan still had every box unticked). One commit — eae57286, which retired the web and wasm stack — resolved 12 of the hardest cases by itself. Survivors carry dated Status sections. It also surfaced real forgotten work: 2026-08-04-issue-28 has three tasks never done, now unblocked, verified against the tree. |
| A40 | M | Incremental parser | `done` | Measured, and the finding was right for the wrong reason: the floor is 23 full-document traversals, not ~4, six of them whole-tree REBUILDS. Cost is Theta(n) — log-log slope 1.07, flat us/KiB across a 26x size range — and a no-op edit costs within 1% of a real one, so 100% of it is edit-independent. The incremental path is currently 2.6-3.6x SLOWER than simply reparsing the document, with parse_window (the only actual parsing) at 1.6% of the time. One provably dead pass removed for ~10%; the rest is structural, because GreenText::SourceSlice holds an absolute range plus the owning SourceText, so any edit changes the document Arc and every token must be rebuilt. Benchmark at crates/waml-syntax/benches/markdown_reparse.rs, analysis in docs/design/incremental-reparse-cost.md. Deliberately not a CI gate. |
| A41 | M | Workspace hygiene | `wip` | 65 fully-merged local branches deleted (100 -> 35 refs); the 15 unmerged branches, 16 untouched stashes and the orphaned worktree husk are now inventoried in docs/reviews/workspace-inventory.md. Stashes are deliberately untouched — only their author can judge an interrupted thought. Root .test_out5.log removed. OWNER DECISION: land or delete the 15 unmerged branches, and drop the stashes that are superseded. |
| A42 | M | OKF spec governance | `decision` | The audit assumed an upstream OKF; the repo records none. Both spec commits (d82c351e v0.1, ddcc0dc0 v0.2) are the owner's own and cite no external revision, so 'no upstream recorded' is indistinguishable from 'no upstream exists' — which is why it survived two versions. The spec now carries a provenance note stating the ambiguity and what to do under each answer. OWNER DECISION: is OKF external (then record URL+SHA and add a CI drift check) or first-party (then the WAML profile is an extension, not a fork awaiting upstreaming)? |
| A43 | L | DiagCode | `done` | 63 variants (not 80) now declare their wire name exactly once, in a macro_rules table that emits the enum, the serde rename, as_str and DiagCode::ALL from the same literal. The two lists did NOT already disagree, but they were mechanically independent — the container carried rename_all while as_str spelled all 63 by hand. Names verified byte-identical to before. A nameless variant is now a compile error and a renamed one fails an exhaustive test; both were confirmed by deliberately breaking them. |
| A44 | L | Solver wire | `done` | EdgeId(usize) — the caller's position in the edge list handed to the router, assigned at intake rather than on model::Edge, because a model edge outlives a solve and the router's input is a filtered projection of it. Correction to the finding: Route::key was None on 100% of structural routes, so the cursor never matched on it at all — it matched endpoints, and the comment calling that a key mismatch was wrong. The fallback it guarded returned centre-to-centre, which strokes a connector straight through both boxes, and one firing anywhere silently disabled label rerouting for the whole diagram. A probe over 13 real diagrams found 0 mismatches, so it was unreachable dead compensation rather than a live defect — now deleted along with routable_edge_indices, a second copy of the router's skip rule. No golden moved. Note: Route now serializes edge:<number> where key:<string> used to be. |
| A45 | L | Naming | `done` | waml-syntax's 'domain-neutral' headline now states the real trade: the core is language-parameterised, the crate exports the WAML island types on purpose because island recognition happens inside the one Markdown parse. The nav.rs/navigation.rs collision and the NavCategory/RowKind mirror ride with the shell refactor (A15). |
| A46 | L | Dead API | `done` | Deleted. The doc comment I added earlier claimed it existed for external callers of the library — but the crate is publish = false at version 0.0.0 with no release tags, so there are no external callers and cannot be. What was left was six lines with no callers on a path that had already drifted from validate() once. git restores it if a caller ever appears. |
| A47 | L | List primitives | `done` | FOUR copies, not three: the tree panel, the overlay panel, the book surface and the linear popup menu each spelled the same thumb arithmetic. One ScrollbarGeometry now answers both halves — the rect that is painted and the inverse a drag chases — with track position, inset and thumb WIDTH as parameters, because 6px in the tree and 4px in the overlay is a per-surface look decision rather than drift. Six tests pin the shared geometry, including that a drag to a painted thumb's position reproduces its scroll exactly (the property four copies could each break separately). One latent inconsistency surfaced and is fixed: the menu measured its content WITH the vertical pad but its track WITHOUT, so an unscrollable menu still computed a thumb; its thumb is now a few pixels taller at a given size, which is the only visible change. |
| A48 | L | Arc-identity rename | `done` | The Arc-identity rule is now enforced through the OP layer, where a future op could break it: every_move_op_preserves_the_text_arc drives concept.move and directory.rename and asserts a Renamed invalidation with no Removed. Verified to bite — rebuilding the bundle from pairs inside pkg.move (fresh Arcs, equal text) makes it fail with exactly the documented degradation, Removed+Inserted in place of Renamed. |
| A49 | L | Property spread | `done` | Five properties now drive the REAL run_batch pipeline (prepare_candidate -> Batch::lower -> prepare_candidate -> to_pairs -> cold re-read), not a test-only API: no panic and no half-written batch, to_pairs round-trips, the writer's incremental validation agrees with a cold reparse, a successful op can point at its own effect, and re-application converges. It found two pre-existing unbounded-growth defects. place.set duplicated its ## Layout line on every call for any QUALIFIED slug — placement_matches built './{slug}.md' while the writer emits a diagram-relative href — so the drag-to-place flow appended forever and place.rm silently removed nothing. pkg.reorder/sort/retitle appended a second member listing whenever the index held one stale entry, which is exactly what pkg.move leaves behind. Two apparent failures were argued down rather than patched: transition.trace.edit addresses traces by list index so idempotence would be the wrong demand, and a batch that relocates documents translates the tree rather than reaching a fixed point. Three seeds committed, each verified against the final generator with only its own fix reverted. |
| A50 | L | Extension packaging | `done` | The extension packages: pnpm run package builds and produces a .vsix (verified, 13 files). Running vsce for real surfaced two blockers the finding did not mention — vsce refuses to run with both a .vscodeignore and a files field ('does not support combining both strategies'), and it wants a LICENSE in the extension directory. files is gone, .vscodeignore is now the only guard between sources and the marketplace, and the packaging test was rewritten to pin THAT mechanism rather than the one it replaced. The bundled-binary branch is no longer dead: scripts/bundle-server.mjs copies the release binary into server/, which is what that branch has always been looking for. OWNER DECISION REMAINS: no publisher id is set, because that is a marketplace account identity, not a code choice — packaging works without it, publishing will not. |
| A51 | L | Manifest honesty | `done` | waml-editor's description now says what it is — a native GPU editor, not a read-only viewer. |
| A52 | L | classifier_page | `done` | The 'a CLI subcommand can emit the identical page' claim now says no CLI consumer exists and no parity test pins it. |
| A53 | L | Parser fn size | `done` | Measured first, and the audit had the wrong file: the biggest parser function in the repo is parse_inlines_within at 458 lines in waml-syntax/src/markdown/inline.rs, not anything in the UML parser. Seven split where the seam was real — flow_transition stated ONE rule three times including the span accounting b0c7e592 had to fix four copies of; layout_statement was two phases with a data seam a comment had already drawn; parse_link opened with a seven-element tuple binding read by loose names for 150 lines. Six deliberately left: parse_strict and collect_queries are flat exhaustive matches that splitting would only scatter, and the remaining UML functions are grammar transcripts whose split would thread the owned/p accounting that keeps the island tree byte-exact through many small functions. |
